numbers = str(input())
lenght = len(numbers)
numbers = numbers.split()
text = ''
for i in range(0, lenght):
    num = str(numbers[i])
    numbers[i] = (numbers[i].replace('0', '*')
    (numbers[i].replace('1', '*'))
    (numbers[i].replace('2', '*'))
    (numbers[i].replace('3', '*'))
    (numbers[i].replace('4', '*'))
    (numbers[i].replace('5', '*'))
    (numbers[i].replace('6', '*'))
    (numbers[i].replace('7', '*'))
    (numbers[i].replace('8', '*'))
    (numbers[i].replace('9', '*')))
    print(numbers[i])
    text = ''
            
